import 'package:flutter/material.dart';

class DlButton extends StatelessWidget {
  const DlButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
